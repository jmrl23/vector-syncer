# secrets & runtime state
.env
data/

# node / worker
node_modules/
dist/
.yarn/cache/
.pnp.*
*.log

# python / converter
__pycache__/
.venv/
.pytest_cache/

# graphify — machine-local knowledge graph, not tracked
graphify-out/

# personal Claude Code overrides (shared hooks in .claude/settings.json stay tracked)
.claude/settings.local.json
