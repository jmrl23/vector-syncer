# secrets & runtime state
.env
data/

# node / worker
node_modules/
dist/
.yarn/cache/
.pnp.*
*.log

# python / converter
__pycache__/
.venv/
.pytest_cache/

# graphify — keep graph.json, GRAPH_REPORT.md, graph.html; ignore machine-local internals
graphify-out/.graphify_*
graphify-out/cache/
graphify-out/cost.json
graphify-out/manifest.json

# personal Claude Code overrides (shared hooks in .claude/settings.json stay tracked)
.claude/settings.local.json
